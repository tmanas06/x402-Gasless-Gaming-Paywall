# Agent Private Key (required)
# Generate a new private key using: npm run generate-key
# Or use an existing wallet's private key
# Format: 0x followed by 64 hex characters (e.g., 0x1234...abcd)
AGENT_PRIVATE_KEY=your_private_key_here

# Cronos RPC URL
CRONOS_RPC=https://evm-t3.cronos.org

# Game API URL (backend server)
GAME_API_URL=http://localhost:5000

# Payment Rules
MAX_PAYMENT_PER_TX=0.05
DAILY_SPENDING_LIMIT=0.50
AUTO_PAY_ENABLED=true

# AI Configuration
# Options: "cronos" (rule-based only), "groq" (AI-only), "both" (rules + AI)
AI_TYPE=both

# GROQ AI Configuration (required if AI_TYPE is "groq" or "both")
# Get your API key from https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-70b-versatile
