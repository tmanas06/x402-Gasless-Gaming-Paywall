# Next.js Public Environment Variables
# These are exposed to the browser

# Privy Authentication (REQUIRED)
# Get these from https://dashboard.privy.io/
# 1. Create a new app or select an existing one
# 2. Go to Settings > API Keys
# 3. Copy the App ID and Client ID
NEXT_PUBLIC_PRIVY_APP_ID=your_privy_app_id_here
NEXT_PUBLIC_PRIVY_CLIENT_ID=your_privy_client_id_here

# Note: JWKS endpoint is only needed for backend token verification
# If you're only using frontend auth, you don't need it
# JWKS endpoint format: https://auth.privy.io/api/v1/oidc/.well-known/jwks.json?app_id=YOUR_APP_ID

# Optional: API URLs (if you need to expose backend URL to frontend)
# NEXT_PUBLIC_API_URL=http://localhost:5000
